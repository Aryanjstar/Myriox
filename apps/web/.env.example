# Auth Service (email/password signup + login, issues the JWT used by the other services)
NEXT_PUBLIC_AUTH_BASE_URL=http://localhost:8030

# Plan Service (plan CRUD + persona catalog)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8010
# Simulation Service (live multi-agent run over WebSocket)
NEXT_PUBLIC_API_WS_URL=ws://localhost:8020
